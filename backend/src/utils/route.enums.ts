export enum COMMON_ROUTE {
  api = "/api",
  blog = "/blog",
}

export enum BLOG_ROUTE {
  create = "/create",
  list = "/list",
  getOne = "/get/:id",
  updateOne = "/update/:id",
  delete = "/delete/:id",
}
