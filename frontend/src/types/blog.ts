export type blogType = {
  _id: string;
  title: string;
  category: string;
  description: string;
  slug: string;
  createdAt: string;
  updatedAt: string;
};

export type blogForm = {
  title: string;
  category: string;
  description: string;
  slug: string;
};
